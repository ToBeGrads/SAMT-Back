pip install -r requirements.txt # or you can activate directly the env pushed SamtBack

# run migration to create the models 
python manage.py makemigrations
python manage.py migrate

# first to add the predefined users to the database 
python manage.py loaddata users.json

